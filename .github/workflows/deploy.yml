name: Multi Domain Deployment (FTP)

on:
  push:
    branches:
      - main    # deploy when pushing to main

env:
  LOCAL_DIR: dist/     # React = build/ , Vite/Vue = dist/

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-dir: ${{ steps.export.outputs.BUILD_DIR }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install & Build
        run: |
          npm ci
          npm run build

      - name: Export Build Dir
        id: export
        run: echo "BUILD_DIR=${{ env.LOCAL_DIR }}" >> $GITHUB_OUTPUT

  # ================================================================
  #          DEPLOY 1 — stage1.grafizen.in (grafizen.in)
  # ================================================================
  deploy_stage1_grafizen:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy → stage1.grafizen.in
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST_GRA }}
          username: ${{ secrets.FTP_USER_GRA }}
          password: ${{ secrets.FTP_PASS_GRA }}
          port: 21
          protocol: ftp
          local-dir: ./${{ needs.build.outputs.build-dir }}
          server-dir: /domains/grafizen.in/public_html/shreegiriraj/
          dangerous-clean-slate: true

  # # ================================================================
  # #      DEPLOY 2 — shreegiriraj.darshit.global (darshit.global)
  # # ================================================================
  # deploy_shreegiriraj_darshit:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v3

  #     - name: Deploy → shreegiriraj.darshit.global
  #       uses: SamKirkland/FTP-Deploy-Action@v4.3.4
  #       with:
  #         server: ${{ secrets.FTP_HOST_DAR }}
  #         username: ${{ secrets.FTP_USER_DAR }}
  #         password: ${{ secrets.FTP_PASS_DAR }}
  #         port: 21
  #         protocol: ftp
  #         local-dir: ./${{ needs.build.outputs.build-dir }}
  #         server-dir: /domains/darshit.global/public_html/shreegiriraj/
  #         dangerous-clean-slate: true

  # # ================================================================
  # #      DEPLOY 3 — shreegiriraj.feedbacks.live (feedbacks.live)
  # # ================================================================
  # deploy_shreegiriraj_feedbacks:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v3

  #     - name: Deploy → shreegiriraj.feedbacks.live
  #       uses: SamKirkland/FTP-Deploy-Action@v4.3.4
  #       with:
  #         server: ${{ secrets.FTP_HOST_FEED }}
  #         username: ${{ secrets.FTP_USER_FEED }}
  #         password: ${{ secrets.FTP_PASS_FEED }}
  #         port: 21
  #         protocol: ftp
  #         local-dir: ./${{ needs.build.outputs.build-dir }}
  #         server-dir: /domains/shreegiriraj.feedbacks.live/public_html/
  #         dangerous-clean-slate: true
